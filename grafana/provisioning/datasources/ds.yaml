apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: app
    database: emoji_chat
    secureJsonData:
      password: $DB_PASSWORD
    jsonData:
      sslmode: 'disable'
      encrypt: 'disable'
    isDefault: true
